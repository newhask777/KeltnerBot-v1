zomro:
# Подключение
ssh root@193.169.105.19
# Пароль
Laravel_888999

# Запуск сервиса
sudo systemctl daemon-reload
sudo systemctl start huma_bot.service
sudo systemctl status huma_bot.service

# Просмотр логов в реальном времени:
journalctl -u huma_bot.service -f

# Просмотр последних 100 сообщений:
journalctl -u huma_bot.service -n 100

# Проверка статуса:
systemctl status huma_bot.service